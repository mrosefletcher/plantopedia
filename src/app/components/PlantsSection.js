import React from 'react';
import styled from '@emotion/styled';
import {SectionBackground, SectionContent} from '@/app/components/Section';


export const PlantSection = () => {
    return (
        <SectionBackground>
            <SectionContent>
            </SectionContent>
        </SectionBackground>
    );
}